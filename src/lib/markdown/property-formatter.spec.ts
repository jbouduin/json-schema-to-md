// import { Schema } from '../schema/schema';
// import { AttributeFormatter } from './attribute-formatter';
// import { PropertyFormatter } from './property-formatter';
// import { SchemaFormatter } from './schema-formatter';

test('first test', () => {
  // const map = new Map<string, Schema>();
  // const required = ["first"];
  // map.set('first', new Schema('dir', 'slug', '{ "title": "title 1", "type": "array" }'));
  // map.set('second', new Schema('dir', 'slug', '{ "title": "title 2", "type": "string" }'));
  // map.set('third', new Schema('dir', 'slug', '{ "title": "title 3", "type": [ "string", "null" ] }'));
  // map.set('fourth', new Schema('dir', 'slug', '{ "title": "title 4", "type": [ "string", "number" ] }'));
  // map.set('fifth', new Schema('dir', 'slug', '{ "type": [ "string", "number", "null" ], "description": "Here comes the description of the property named fifth" }'));
  // map.set('sixht', new Schema('dir', 'slug', '{ "type": [ "string", "number", "null" ], "$id": "#/properties/sixth" }'));

  // const x = new PropertyFormatter().format(new SchemaFormatter(), new AttributeFormatter(), schema);
  // console.log(x);
})